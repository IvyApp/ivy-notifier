<p class="lead">
  What follows is a live feed of public builds from
  <a href="https://travis-ci.org/recent">Travis CI</a>.
</p>

<ul>
  {{#each build in this itemController="build"}}
    <li>
      <span {{bind-attr class=":pull-right :label build.labelClass"}}>
        {{build.model.state}}
      </span>
      <p>
        {{build.model.commit.committerName}} pushed to
        {{build.model.commit.branch}} at
        <a {{bind-attr href=build.commit.compareURL}}>{{build.model.repository.slug}}</a>
      </p>
      <p class="small text-muted">
        <a {{bind-attr href=build.commit.compareURL}}>{{build.model.commit.shortSha}}</a>
        {{build.model.commit.message}}
      </p>
    </li>
  {{/each}}
</ul>
